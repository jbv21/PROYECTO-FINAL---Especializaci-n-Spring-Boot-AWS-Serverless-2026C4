# =============================================================================
# application.yml -- Configuracion comun (Orchestrator v3)
# =============================================================================
spring:
  main:
    banner-mode: off
    web-application-type: servlet
  application:
    name: msg-pipeline-orchestrator-v3

logging:
  level:
    root: INFO
    com.msgpipeline: INFO
    software.amazon.awssdk: WARN
